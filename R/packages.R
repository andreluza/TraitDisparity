rm(list=ls())

# go through folders and files
require(here)

# morphometrics analysis
require(geomorph)
require(mvMORPH)

# phylogenetic and functional analysis
require(ape)
require(SYNCSA)
require(geiger)
require(picante)

# parallel processing
require(parallel)

# plot
require(ggplot2)
require(reshape)
