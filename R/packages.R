

rm(list=ls())

# go through folders and files
require(here)

# morphometrics analysis
require(geomorph)
require(mvMORPH)

# phylogenetic and functional analysis
require(ape)
require(SYNCSA)
require(geiger)
require(picante)

# parallel processing
require(parallel)

# plot
require(ggplot2)
require(reshape)
require(gridExtra)
require (ggpubr)
require(ggthemes)

# spatial analysis

library(sp)
library(raster)
library (rgdal)
require(maps)

# maps
library(rasterVis)
library(viridis)

# table
library("sjPlot")
library("sjmisc")
library("sjlabelled")

# analysis
library(emmeans)

